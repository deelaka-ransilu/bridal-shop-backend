package edu.bridalshop.backend.enums;

public enum StockUnit {
    PCS,    // Pieces (for dresses, spare parts)
    METER,  // Meters (for fabric/cloth)
    ROLL,   // Rolls (for fabric rolls)
    PAIR,   // Pairs (for accessories)
    HOUR    // Hours (for services)
}
